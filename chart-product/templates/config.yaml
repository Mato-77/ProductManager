apiVersion: v1
kind: ConfigMap
metadata:
  name:  {{ Release.Name }}-config
data:
  postgre-url: {{ .Values.configs.postgre-url }}
  postgre-ddl: {{ .Values.configs.postgre-ddl }}
  backend-url: {{ .Values.configs.backend-url }}
