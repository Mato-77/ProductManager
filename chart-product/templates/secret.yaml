apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
data:
  postgre-user: {{ .Values.secrets.postgre-user | b64enc }}
  postgre-password: {{ .Values.secrets.postgre-password | b64enc }}
  postgre-db: {{ .Values.secrets.postgre-db| b64enc }}
