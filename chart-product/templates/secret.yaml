apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
data:
  postgre_user: {{ .Values.secrets.postgre_user | b64enc }}
  postgre_password: {{ .Values.secrets.postgre_password | b64enc }}
  postgre_db: {{ .Values.secrets.postgre_db| b64enc }}
