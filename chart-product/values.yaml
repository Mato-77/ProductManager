# Default values for chart-product.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

spring:
  repository: mato11231/store-backend
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"
  containerName: "spring"
  app: "spring"
  name: "spring"

postgre:
  repository: postgres
  pullPolicy: Always
  tag: "13.2-alpine"
  containerName: "database"
  name: "postgre"
  app: "postgre"

react:
  repository: mato11231/store-frontend
  pullPolicy: Always
  tag: "latest"
  containerName: "react"
  name: "react"
  app: "react"




serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

spring_service:
  type: ClusterIP
  port: 8080


react_service:
  type: NodePort
  port: 3000
  targetPort: 3000
  nodePort: 3007
  selector:
    app: react


postgre_service:
  type: ClusterIP
  port: 5432

service:
  type: ClusterIP
  port: 80


ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

secrets:
  postgre_user: "UHJvZWt0S0lJCg=="
  postgre_password: "bWF0bwo="
  postgre_db: "c3RvcmU="

configs:
  backend_url: "react_service"
  postgre_url: "jdbc:postgresql://postgre_service/store?currentSchema=public"
  postgre_ddl: "update"


